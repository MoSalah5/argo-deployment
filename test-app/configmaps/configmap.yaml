apiVersion: v1
kind: ConfigMap
metadata:
  name: test-config
data:
  config.json: |
    {
      "environment" : "dev"
    }
# kubectl create configmap test-config --from-file ./golang/configs/config.json
